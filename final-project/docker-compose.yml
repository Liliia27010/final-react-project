services:
  backend:
    build: ./backend
    ports:
      - "5001:5001"
    volumes:
      - ./db:/app/db
    environment:
      - PORT=5001

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
